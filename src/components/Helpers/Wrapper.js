// Not require to import react because we not going to write any jsx code in their, instead of just treat my wrapper component. This really empty component.

const Wrapper = (props) => {
    return props.children;
};
export default Wrapper;

